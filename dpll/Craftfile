# craftr_module(nr.c_snippets.dpll)

load_module('compiler')
c = compiler.CCompiler()
c.detect()

objects = c.objects(
  sources = local(['main.c', 'dpll.c']),
  warning_level = 'all',
)

main = c.executable(
  filename = 'dpll',
  inputs = [objects],
)
