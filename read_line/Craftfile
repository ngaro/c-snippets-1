# craftr_module(nr.c_snippets.read_line)

load_module('compiler')
c = compiler.CCompiler()

objects = c.objects(
  sources = local(['main.c']),
)

main = c.executable(
  filename = 'main',
  inputs = [objects],
  default = True,
)

